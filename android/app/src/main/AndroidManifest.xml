<?xml version="1.0" encoding="utf-8"?>
<!--
    AndroidManifest.xml
    
    Application manifest for Limitless Companion Android app.
    
    Declares:
    - Required permissions
    - Application components (activities, services)
    - Hardware requirements
    
    TODO(milestone-1): Add backup rules XML files
    TODO(milestone-2): Configure app shortcuts
    TODO(milestone-3): Add widget declarations if needed
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ============================================ -->
    <!-- Network Permissions -->
    <!-- ============================================ -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- ============================================ -->
    <!-- Audio Recording Permissions -->
    <!-- ============================================ -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- ============================================ -->
    <!-- Bluetooth Permissions -->
    <!-- ============================================ -->
    <!-- Legacy Bluetooth permissions (API < 31) -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    
    <!-- New Bluetooth permissions (API >= 31) -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />

    <!-- ============================================ -->
    <!-- Notification Permissions (API >= 33) -->
    <!-- ============================================ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- ============================================ -->
    <!-- System Permissions -->
    <!-- ============================================ -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <!-- ============================================ -->
    <!-- Hardware Features (Optional) -->
    <!-- ============================================ -->
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.bluetooth"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LimitlessCompanion"
        android:usesCleartextTraffic="false"
        tools:targetApi="31">

        <!-- ============================================ -->
        <!-- Main Activity -->
        <!-- ============================================ -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.LimitlessCompanion"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            
            <!-- TODO(milestone-2): Add deep link support -->
            <!-- <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="limitless" android:host="open" />
            </intent-filter> -->
        </activity>

        <!-- ============================================ -->
        <!-- Services -->
        <!-- ============================================ -->
        
        <!-- Audio Capture Service (Foreground Service) -->
        <service
            android:name=".services.AudioCaptureService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="microphone"
            android:stopWithTask="false" />

        <!-- ============================================ -->
        <!-- Content Providers -->
        <!-- ============================================ -->
        
        <!-- File Provider for secure file sharing -->
        <!-- TODO(milestone-1): Create file_paths.xml -->
        <!-- <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider> -->

        <!-- ============================================ -->
        <!-- Work Manager -->
        <!-- ============================================ -->
        
        <!-- TODO(milestone-2): Configure WorkManager -->
        <!-- <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider> -->

    </application>

</manifest>